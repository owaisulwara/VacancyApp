import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  logoContainer:{
    alignSelf:'center',
  },
  logo: {
    height: 80,
    width: 202,
    marginTop:50,
    marginBottom:30,
  }
});

export default styles;
