import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btnClickContain: {
    flex:1,
    alignItems:'center',
    alignSelf:'center'
  },
  btnContainer: {
    flex: 1,
  },
  btnIcon: {
    height: 50,
    width: 50
  },
  btnText: {
    fontSize: 12,
    textAlign:'center'
  }
});

export default styles;
